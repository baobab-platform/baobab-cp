// FE-01 foundation only. The shell, sign-in and workspaces arrive in later
// gates (docs/frontend/fe-00-architecture-lock.md section 9); this page
// shows no data, so it cannot show invented data (prompt section 102).
export default function Home() {
  return (
    <main>
      <h1>Baobab Control Plane Console</h1>
      <p>The Console is being built. No administrative functions are available yet.</p>
    </main>
  );
}

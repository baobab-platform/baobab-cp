schema_version: 1
repository:
  lifecycle: active
capabilities:
  - go
  - node
  - container
  - engine
environment:
  baobab_dev:
    required: true
    profile: full
artifacts:
  container:
    enabled: true
    dockerfile: Dockerfile
    context: .
    runtime: true
security:
  sast_provider: codeql
